export enum Headers {
  ContentType = "Content-Type",
}

export enum MediaType {
  JsonAPI = "application/vnd.api+json",
}

export enum TokenType {
  AnyOperator,
  Comma,
  Condition,
  L_PAREN,
  Literal,
  Negate,
  Operator,
  R_Condition,
  R_PAREN,
  S_QUOTE,
  String,
}
